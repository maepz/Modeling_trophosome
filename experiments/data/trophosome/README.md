# Curated experiment data

Only verified, immutable release material belongs here. Active checkpoints,
intermediate outputs, failed runs and scheduler logs remain in external
scratch.

Every future release must include a manifest, source and configuration
provenance, and checksums before scratch data are removed.
